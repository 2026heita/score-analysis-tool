# 通用表格数据分析平台 - 产品与技术差距分析

**分析日期**: 2026-07-20  
**最后更新**: 2026-07-21  
**分析版本**: v1.9.2 (最新提交: 8a8bd4b)  
**分析目标**: 评估当前平台是否真正达到"通用表格分析"定位

---

## 一、产品定位核验

### 1.1 当前定位声明

**目标定位**: 通用表格数据分析平台  
**实际状态**: ⚠️ **部分达到，存在教育领域污染**

### 1.2 教育领域概念污染清单

#### 1.2.1 字段分类系统（核心污染）

**文件**: `src/utils/tableParser/fieldClassifier.ts`

**问题**:
- 包含大量教育专用关键词：学号、班级、语文、数学、英语、物理、化学、生物、政治、历史、地理、总分、成绩、分数、排名、名次等
- `SCORE_KEYWORDS` 数组包含 27 个教育相关关键词
- `IDENTITY_KEYWORDS` 数组包含 18 个教育身份关键词（学校代码、考号、座号等）
- `RANK_KEYWORDS` 数组包含 9 个排名字段关键词
- `CATEGORY_KEYWORDS` 数组包含 10 个教育分类关键词（组合、科类、选科等）

**影响**:
- 字段分类强烈偏向教育数据
- 销售、财务、实验数据可能被错误分类
- 通用字段（如"金额"、"日期"、"数量"）缺乏关键词支持

**推荐处理**: 参数化  
**修改风险**: 中 - 需要重构字段分类逻辑，建立通用字段类型系统

---

#### 1.2.2 指标方向硬编码

**文件**: `src/engine/metricLayer.ts` (第 88-90 行)

**问题**:
```typescript
function getMetricDirection(role: string): MetricDirection {
  // rank 字段：越小越好
  return role === 'rank' ? 'lower-is-better' : 'higher-is-better';
}
```

**当前行为**:
- 所有 `rank` 类型字段默认为 `lower-is-better`
- 其他字段默认为 `higher-is-better`

**影响**:
- 对于销售数据，"排名"可能是"销售额排名"（越高越好）
- 对于运营数据，"退款率排名"应该是越低越好，但系统无法区分
- 用户无法自定义指标方向

**推荐处理**: 参数化  
**修改风险**: 低 - 添加用户可配置的指标方向选项

---

#### 1.2.3 导出文本硬编码

**文件**: `src/engine/exportAnalysis.ts` (第 157 行)

**问题**:
```typescript
lines.push(buildCsvLine(['方向', metricResult.direction === 'higher-is-better' ? '越高越好' : '越低越好']));
```

**当前行为**:
- 导出 CSV 时使用中文"越高越好/越低越好"
- 硬编码教育场景的判断逻辑

**影响**:
- 通用平台不应有固定的方向判断文本
- 用户可能导出非教育数据，但导出内容仍显示"越高越好"

**推荐处理**: 参数化  
**修改风险**: 低 - 使用字段元数据或用户配置替代硬编码

---

#### 1.2.4 解析报告字段推荐

**文件**: `src/hooks/useParsedTable.ts` (第 76-81 行)

**问题**:
```typescript
const recommendedFields = parseSummary.fieldTypes
  .filter(meta => {
    const role = meta.analysisRole;
    return role === 'primaryTotal' || role === 'rank' || role === 'sectionTotal' || role === 'courseScore';
  })
  .map(meta => meta.header);
```

**当前行为**:
- 推荐字段仅包含教育相关角色：primaryTotal（总分）、rank（排名）、sectionTotal（合计）、courseScore（课程成绩）
- 通用数值字段（如"销售额"、"库存量"）不会被推荐

**影响**:
- 非教育数据的用户需要手动选择分析字段
- 系统无法识别通用数据的核心指标

**推荐处理**: 参数化  
**修改风险**: 中 - 需要重构字段推荐逻辑

---

#### 1.2.5 语义层维度关键词

**文件**: `src/engine/metricLayer.ts` (第 117 行)

**问题**:
```typescript
const DIMENSION_KEYWORDS = ['班级', '学校', '部门', '组别', '类别', '科类', '选科', '组合'];
```

**当前行为**:
- 维度识别关键词偏向教育（班级、学校、科类、选科）
- 通用维度（如"地区"、"产品线"、"客户类型"）缺乏支持

**影响**:
- 分组分析时，通用维度字段可能无法正确识别

**推荐处理**: 参数化  
**修改风险**: 低 - 扩展维度关键词列表

---

#### 1.2.6 主键识别关键词

**文件**: `src/engine/metricLayer.ts` (第 139-142 行)

**问题**:
```typescript
const isIdField = headerLower.includes('学号') || 
                  headerLower.includes('考号') || 
                  headerLower.includes('编号') ||
                  headerLower.includes('id');
```

**当前行为**:
- 主键识别偏向教育（学号、考号）
- 通用主键（如"订单号"、"客户ID"、"产品编码"）支持不足

**影响**:
- 非教育数据的主键字段可能无法正确识别

**推荐处理**: 参数化  
**修改风险**: 低 - 扩展主键关键词列表

---

#### 1.2.7 示例数据（已改善）

**文件**: `src/data/sampleDatasets.ts`

**当前状态**: ✅ **已改善**
- 原教育成绩样例已移除
- 替换为通用调研数据、电商运营、销售业绩、用户行为、国家统计、游戏数值等 6 类示例

**影响**:
- 用户可以看到多领域示例数据
- 但仍需验证分析引擎对这些数据的支持程度

---

### 1.3 通用能力与教育专用能力依赖关系

#### 1.3.1 核心分析引擎（通用能力）

**文件**:
- `src/engine/analysisEngine.ts` - 统一分析引擎
- `src/engine/schemaDetector.ts` - 通用 Schema 检测
- `src/engine/correlationAnalyzer.ts` - 相关性分析
- `src/engine/groupByDimension.ts` - 分组统计
- `src/engine/filterRows.ts` - 数据筛选

**状态**: ✅ **基本通用**
- 统计计算逻辑无领域偏见
- Schema 检测器无教育关键词
- 支持任意数值字段的分析

**问题**:
- `analysisEngine.ts` 中的 `isRankField()` 函数使用教育关键词判断排名字段
- 依赖 `fieldClassifier.ts` 的分类结果，间接受到教育污染

---

#### 1.3.2 字段分类系统（教育专用）

**文件**:
- `src/utils/tableParser/fieldClassifier.ts` - 字段分类
- `src/utils/tableParser/contentAnalyzer.ts` - 内容分析
- `src/utils/tableParser/types.ts` - 类型定义

**状态**: ⚠️ **强教育偏向**
- `FieldType` 类型定义包含教育专用类型：`score`、`bonus`、`penalty`
- `AnalysisRole` 类型定义包含教育专用角色：`primaryTotal`、`courseScore`、`adjustment`
- 关键词列表几乎全部来自教育领域

**影响**:
- 整个字段分类链路（解析 → 分类 → 语义层 → 分析）都受到教育污染
- 通用数据需要绕过教育分类逻辑才能正确分析

---

#### 1.3.3 教育专用模块（已隔离）

**文件**:
- `src/features/legacy/education/TraditionalSubjectRadar.tsx` - 传统成绩雷达图
- `src/config/education.ts` - 教育配置（已标记 @deprecated）

**状态**: ✅ **已隔离**
- 教育专用功能已迁移至 `features/legacy/education` 目录
- 主链路不再依赖教育专用模块
- `TraditionalSubjectRadar.tsx` 仅在特定条件下使用

**问题**:
- `TraditionalSubjectRadar.tsx` 仍包含硬编码的科目顺序
- 文件过长（2307 行），职责过多

---

### 1.4 当前默认界面感知

**问题**: 当前默认界面是否让普通用户感觉这是一个成绩分析工具？

**分析**:
1. **字段推荐逻辑**: 推荐"总分"、"排名"、"课程成绩"等教育字段 → ⚠️ 教育偏向
2. **示例数据**: 已替换为通用数据 → ✅ 通用
3. **使用说明**: 未检查，需验证是否有教育术语
4. **界面文案**: 未检查，需验证是否有"成绩"、"学生"等词汇

**结论**: ⚠️ **部分界面仍暗示教育工具**
- 字段推荐逻辑偏向教育
- 需要全面审查界面文案

---

## 二、完整用户流程分析

### 2.1 用户流程图谱

```
1. 文件上传/数据粘贴
   ↓
2. 文件和工作表选择
   ↓
3. 表头识别
   ↓
4. 字段类型识别（⚠️ 教育偏向）
   ↓
5. 解析报告（⚠️ 推荐教育字段）
   ↓
6. 数据概览
   ↓
7. 字段选择（⚠️ 默认推荐教育字段）
   ↓
8. 单变量分析
   ↓
9. 分组分析
   ↓
10. 关系分析
   ↓
11. 异常值处理
   ↓
12. 筛选
   ↓
13. 导出（⚠️ 仅 CSV）
   ↓
14. 状态保存和恢复
```

### 2.2 逐步分析

#### 步骤 1: 文件上传或数据粘贴

**当前能力**:
- ✅ 支持 XLSX、XLS、CSV、文本粘贴
- ✅ 文件大小限制 20MB
- ✅ 多工作表支持

**用户需要理解的概念**:
- 文件格式
- 文件大小限制

**问题**:
- ❌ 无文件预览
- ❌ 无文件格式自动检测提示

**通用性**: ✅ **通用**

---

#### 步骤 2: 文件和工作表选择

**当前能力**:
- ✅ 多工作表切换
- ✅ 工作表智能检测（基于教育关键词）

**用户需要理解的概念**:
- 工作表

**问题**:
- ⚠️ 工作表检测使用教育关键词（`scoreKeywordsCount`）
- ❌ 无工作表预览

**通用性**: ⚠️ **部分通用**
- 工作表检测偏向教育数据

---

#### 步骤 3: 表头识别

**当前能力**:
- ✅ 自动表头检测
- ✅ 多级表头扁平化

**用户需要理解的概念**:
- 表头行
- 多级表头

**问题**:
- ❌ 无表头识别结果展示
- ❌ 用户无法手动修正表头行

**通用性**: ✅ **通用**

---

#### 步骤 4: 字段类型识别

**当前能力**:
- ✅ 字段类型识别（数值、文本、日期、标识符、类别）
- ⚠️ 字段分类偏向教育（score、rank、bonus、penalty）

**用户需要理解的概念**:
- 字段类型
- 字段分类

**问题**:
- ⚠️ 字段分类使用教育关键词
- ❌ 用户无法手动修正字段类型
- ❌ 无字段类型说明

**通用性**: ⚠️ **部分通用**
- 基础类型识别通用
- 字段分类教育偏向

---

#### 步骤 5: 解析报告

**当前能力**:
- ✅ 解析报告展示字段分类结果
- ⚠️ 推荐字段偏向教育

**用户需要理解的概念**:
- 字段分类
- 推荐字段

**问题**:
- ⚠️ 推荐字段逻辑偏向教育（primaryTotal、rank、courseScore）
- ❌ 无推荐原因说明
- ❌ 用户无法修改推荐逻辑

**通用性**: ⚠️ **部分通用**
- 报告展示通用
- 推荐逻辑教育偏向

---

#### 步骤 6: 数据概览

**当前能力**:
- ✅ 数据概览展示
- ✅ 字段统计信息

**用户需要理解的概念**:
- 数据行数
- 字段数量

**问题**:
- ❌ 无数据质量提示（缺失值、异常值比例）
- ❌ 无数据类型分布

**通用性**: ✅ **通用**

---

#### 步骤 7: 字段选择

**当前能力**:
- ✅ 字段选择器
- ⚠️ 默认推荐教育字段

**用户需要理解的概念**:
- 分析字段
- 推荐字段

**问题**:
- ⚠️ 默认推荐教育字段
- ❌ 无字段推荐原因说明
- ❌ 用户无法自定义推荐规则

**通用性**: ⚠️ **部分通用**
- 字段选择功能通用
- 推荐逻辑教育偏向

---

#### 步骤 8: 单变量分析

**当前能力**:
- ✅ 描述性统计（均值、中位数、标准差、四分位数）
- ✅ 百分位计算
- ✅ 排名计算
- ⚠️ 排名方向硬编码（rank 字段默认 lower-is-better）

**用户需要理解的概念**:
- 均值、中位数、标准差
- 百分位
- 排名

**问题**:
- ⚠️ 排名方向硬编码
- ❌ 无统计指标说明
- ❌ 用户无法自定义指标方向

**通用性**: ⚠️ **部分通用**
- 统计计算通用
- 排名方向硬编码

---

#### 步骤 9: 分组分析

**当前能力**:
- ✅ 按维度字段分组统计
- ⚠️ 维度识别偏向教育

**用户需要理解的概念**:
- 维度字段
- 分组统计

**问题**:
- ⚠️ 维度识别使用教育关键词（班级、学校、科类）
- ❌ 无维度识别说明

**通用性**: ⚠️ **部分通用**
- 分组统计功能通用
- 维度识别教育偏向

---

#### 步骤 10: 关系分析

**当前能力**:
- ✅ 相关性分析（Pearson 相关系数）
- ✅ 相关性强弱分类

**用户需要理解的概念**:
- 相关系数
- 相关性强弱

**问题**:
- ❌ 无相关性说明
- ❌ 无散点图展示

**通用性**: ✅ **通用**

---

#### 步骤 11: 异常值处理

**当前能力**:
- ✅ 异常值检测（IQR 方法）
- ✅ 异常值列表展示

**用户需要理解的概念**:
- 异常值
- IQR 方法

**问题**:
- ❌ 无异常值说明
- ❌ 用户无法调整异常值检测阈值

**通用性**: ✅ **通用**

---

#### 步骤 12: 筛选

**当前能力**:
- ✅ 多条件 AND 筛选
- ✅ 筛选条件保存

**用户需要理解的概念**:
- 筛选条件
- AND 逻辑

**问题**:
- ❌ 无筛选条件说明
- ❌ 不支持 OR 逻辑

**通用性**: ✅ **通用**

---

#### 步骤 13: 导出

**当前能力**:
- ✅ CSV 导出（筛选后数据、分组统计、指标摘要）
- ❌ Excel 导出
- ❌ PDF 导出

**用户需要理解的概念**:
- CSV 格式

**问题**:
- ❌ 仅支持 CSV 导出
- ⚠️ 导出文本包含教育偏向（"越高越好/越低越好"）

**通用性**: ⚠️ **部分通用**
- 导出功能通用
- 导出文本教育偏向

---

#### 步骤 14: 状态保存和恢复

**当前能力**:
- ✅ localStorage 自动保存
- ✅ 状态版本迁移

**用户需要理解的概念**:
- 状态保存
- 版本迁移

**问题**:
- ❌ 无状态管理界面
- ❌ 无法导出/导入状态

**通用性**: ✅ **通用**

---

### 2.3 数据变更触发重新计算

**当前行为**:
- 数据变更（文件上传、工作表切换）时，自动触发：
  1. 表头识别
  2. 字段分类
  3. 解析报告生成
  4. 数据概览更新
  5. 分析结果重新计算

**问题**:
- ❌ 用户不清楚当前分析基于原始数据还是筛选后数据
- ❌ 无重新计算进度提示

---

## 三、通用能力差距矩阵

### 3.1 场景 1: 学生成绩数据

| 能力 | 状态 | 说明 |
|------|------|------|
| 表头识别 | ✅ 已可靠支持 | 自动检测表头行 |
| 字段类型识别 | ✅ 已可靠支持 | 教育关键词匹配 |
| 日期处理 | ⚠️ 部分支持 | 支持日期识别，但无日期分析 |
| 缺失值处理 | ✅ 已可靠支持 | 自动过滤无效值 |
| 重复值处理 | ⚠️ 部分支持 | 表头去重，数据行重复未处理 |
| 筛选 | ✅ 已可靠支持 | 多条件 AND 筛选 |
| 分组汇总 | ✅ 已可靠支持 | 按班级、科目分组 |
| 关系分析 | ✅ 已可靠支持 | 科目间相关性 |
| 图表生成 | ✅ 已可靠支持 | 直方图、箱线图、雷达图等 |
| 结果解释 | ⚠️ 部分支持 | 有统计指标，但无业务解释 |
| 导出 | ⚠️ 部分支持 | 仅 CSV，无 Excel |
| 错误/误导 | ⚠️ 存在风险 | 排名方向硬编码 |

**总结**: ✅ **高度支持**，但存在排名方向硬编码问题

---

### 3.2 场景 2: 销售订单数据

| 能力 | 状态 | 说明 |
|------|------|------|
| 表头识别 | ✅ 已可靠支持 | 自动检测表头行 |
| 字段类型识别 | ⚠️ 部分支持 | 无法识别"金额"、"数量"等通用字段 |
| 日期处理 | ⚠️ 部分支持 | 支持日期识别，但无日期分析 |
| 缺失值处理 | ✅ 已可靠支持 | 自动过滤无效值 |
| 重复值处理 | ⚠️ 部分支持 | 表头去重，数据行重复未处理 |
| 筛选 | ✅ 已可靠支持 | 多条件 AND 筛选 |
| 分组汇总 | ⚠️ 部分支持 | 维度识别偏向教育 |
| 关系分析 | ✅ 已可靠支持 | 字段间相关性 |
| 图表生成 | ✅ 已可靠支持 | 直方图、箱线图等 |
| 结果解释 | ❌ 不支持 | 无销售业务解释 |
| 导出 | ⚠️ 部分支持 | 仅 CSV，无 Excel |
| 错误/误导 | ⚠️ 存在风险 | 字段分类错误 |

**总结**: ⚠️ **部分支持**，字段分类和维度识别存在问题

---

### 3.3 场景 3: 问卷调查数据

| 能力 | 状态 | 说明 |
|------|------|------|
| 表头识别 | ✅ 已可靠支持 | 自动检测表头行 |
| 字段类型识别 | ⚠️ 部分支持 | 无法识别 Likert 量表、分类变量 |
| 日期处理 | ❌ 不支持 | 问卷通常无日期 |
| 缺失值处理 | ✅ 已可靠支持 | 自动过滤无效值 |
| 重复值处理 | ⚠️ 部分支持 | 表头去重，数据行重复未处理 |
| 筛选 | ✅ 已可靠支持 | 多条件 AND 筛选 |
| 分组汇总 | ⚠️ 部分支持 | 维度识别偏向教育 |
| 关系分析 | ✅ 已可靠支持 | 问题间相关性 |
| 图表生成 | ⚠️ 部分支持 | 缺少 Likert 量表专用图表 |
| 结果解释 | ❌ 不支持 | 无问卷业务解释 |
| 导出 | ⚠️ 部分支持 | 仅 CSV，无 Excel |
| 错误/误导 | ⚠️ 存在风险 | 字段分类错误 |

**总结**: ⚠️ **部分支持**，缺少问卷专用分析能力

---

### 3.4 场景 4: 实验检测数据

| 能力 | 状态 | 说明 |
|------|------|------|
| 表头识别 | ✅ 已可靠支持 | 自动检测表头行 |
| 字段类型识别 | ⚠️ 部分支持 | 无法识别实验参数、对照组 |
| 日期处理 | ⚠️ 部分支持 | 支持日期识别，但无日期分析 |
| 缺失值处理 | ✅ 已可靠支持 | 自动过滤无效值 |
| 重复值处理 | ⚠️ 部分支持 | 表头去重，数据行重复未处理 |
| 筛选 | ✅ 已可靠支持 | 多条件 AND 筛选 |
| 分组汇总 | ⚠️ 部分支持 | 维度识别偏向教育 |
| 关系分析 | ✅ 已可靠支持 | 参数间相关性 |
| 图表生成 | ⚠️ 部分支持 | 缺少实验专用图表（如箱线图对比） |
| 结果解释 | ❌ 不支持 | 无实验业务解释 |
| 导出 | ⚠️ 部分支持 | 仅 CSV，无 Excel |
| 错误/误导 | ⚠️ 存在风险 | 字段分类错误 |

**总结**: ⚠️ **部分支持**，缺少实验专用分析能力

---

### 3.5 场景 5: 财务收支数据

| 能力 | 状态 | 说明 |
|------|------|------|
| 表头识别 | ✅ 已可靠支持 | 自动检测表头行 |
| 字段类型识别 | ⚠️ 部分支持 | 无法识别"收入"、"支出"、"利润"等 |
| 日期处理 | ⚠️ 部分支持 | 支持日期识别，但无日期分析 |
| 缺失值处理 | ✅ 已可靠支持 | 自动过滤无效值 |
| 重复值处理 | ⚠️ 部分支持 | 表头去重，数据行重复未处理 |
| 筛选 | ✅ 已可靠支持 | 多条件 AND 筛选 |
| 分组汇总 | ⚠️ 部分支持 | 维度识别偏向教育 |
| 关系分析 | ✅ 已可靠支持 | 字段间相关性 |
| 图表生成 | ⚠️ 部分支持 | 缺少财务专用图表（如瀑布图） |
| 结果解释 | ❌ 不支持 | 无财务业务解释 |
| 导出 | ⚠️ 部分支持 | 仅 CSV，无 Excel |
| 错误/误导 | ⚠️ 存在风险 | 字段分类错误 |

**总结**: ⚠️ **部分支持**，字段分类和维度识别存在问题

---

### 3.6 场景 6: 库存或运营数据

| 能力 | 状态 | 说明 |
|------|------|------|
| 表头识别 | ✅ 已可靠支持 | 自动检测表头行 |
| 字段类型识别 | ⚠️ 部分支持 | 无法识别"库存量"、"周转率"等 |
| 日期处理 | ⚠️ 部分支持 | 支持日期识别，但无日期分析 |
| 缺失值处理 | ✅ 已可靠支持 | 自动过滤无效值 |
| 重复值处理 | ⚠️ 部分支持 | 表头去重，数据行重复未处理 |
| 筛选 | ✅ 已可靠支持 | 多条件 AND 筛选 |
| 分组汇总 | ⚠️ 部分支持 | 维度识别偏向教育 |
| 关系分析 | ✅ 已可靠支持 | 字段间相关性 |
| 图表生成 | ⚠️ 部分支持 | 缺少运营专用图表 |
| 结果解释 | ❌ 不支持 | 无运营业务解释 |
| 导出 | ⚠️ 部分支持 | 仅 CSV，无 Excel |
| 错误/误导 | ⚠️ 存在风险 | 字段分类错误 |

**总结**: ⚠️ **部分支持**，字段分类和维度识别存在问题

---

### 3.7 能力差距总结

| 场景 | 支持程度 | 主要问题 |
|------|---------|---------|
| 学生成绩数据 | ✅ 高度支持 | 排名方向硬编码 |
| 销售订单数据 | ⚠️ 部分支持 | 字段分类错误、维度识别错误 |
| 问卷调查数据 | ⚠️ 部分支持 | 字段分类错误、缺少专用图表 |
| 实验检测数据 | ⚠️ 部分支持 | 字段分类错误、缺少专用图表 |
| 财务收支数据 | ⚠️ 部分支持 | 字段分类错误、维度识别错误 |
| 库存运营数据 | ⚠️ 部分支持 | 字段分类错误、维度识别错误 |

**核心差距**:
1. **字段分类系统教育偏向** - 影响所有非教育数据
2. **维度识别教育偏向** - 影响分组分析
3. **指标方向硬编码** - 影响排名计算
4. **缺少 Excel 导出** - 影响用户体验
5. **缺少日期分析能力** - 影响时间序列数据

---

## 四、现有升级建议重新评估

### 4.1 Excel 导出

**原建议**: P1.1 - 引入 exceljs 实现 Excel 导出

**重新评估**:
- ✅ 项目已依赖 `xlsx` (SheetJS)
- ✅ `xlsx` 库已支持 Excel 读写
- ❌ 无需引入 `exceljs`，避免同时维护两套 Excel 库

**新建议**:
- 使用现有 `xlsx` 库实现 Excel 导出
- 支持多工作表、格式化、公式

**优先级**: P1 - 高  
**工作量**: 中  
**修改风险**: 低

---

### 4.2 PDF 导出

**原建议**: P1.2 - 引入 jspdf 实现 PDF 导出

**重新评估**:
- ❌ PDF 导出优先级低于以下能力：
  - 数据清洗（缺失值、重复值处理）
  - 字段类型修正
  - 日期和分类变量分析
  - 分组汇总
  - 分析过程可解释性

**新建议**:
- 降级为 P2，优先补齐基础数据分析能力
- 仅在用户明确需求时实现

**优先级**: P2 - 中  
**工作量**: 中  
**修改风险**: 低

---

### 4.3 假设检验

**原建议**: P1.5 - 实现 t 检验、卡方检验、ANOVA

**重新评估**:
- ❌ 假设检验需要严格的前提条件：
  - 数据类型匹配
  - 样本独立性
  - 正态性检验
  - 方差齐性检验
  - 小样本处理
  - 非参数替代方法
  - 多重比较校正
  - 效应量计算
  - 置信区间
  - 缺失值处理
- ❌ 用户误用风险高
- ❌ 当前项目缺乏统计专家指导

**新建议**:
- 降级为 P3，优先补齐基础数据分析能力
- 实现前需建立统计检验框架
- 提供检验前提条件检查和用户指导

**优先级**: P3 - 低  
**工作量**: 大  
**修改风险**: 中

---

### 4.4 智能字段推荐

**原建议**: P3.1 - 引入 TensorFlow.js 实现智能推荐

**重新评估**:
- ❌ TensorFlow.js 引入成本高
- ✅ 规则系统可通过以下方式实现可靠推荐：
  - 字段语义（关键词匹配）
  - 唯一值数量
  - 数据分布
  - 用户确认机制

**新建议**:
- 优先实现规则系统
- 基于字段类型、唯一值数量、数据分布构建推荐规则
- 提供用户确认机制，允许手动调整

**优先级**: P2 - 中  
**工作量**: 中  
**修改风险**: 低

---

### 4.5 自然语言查询

**原建议**: P3.2 - 集成 NLP 服务实现自然语言查询

**重新评估**:
- ❌ 涉及用户数据隐私
- ❌ API 费用
- ❌ 网络依赖
- ❌ 密钥管理
- ❌ 错误操作风险
- ❌ 不可复现分析

**新建议**:
- 不列入近期开发范围
- 仅在用户明确需求且有充分理由时实现

**优先级**: P3 - 低（暂不实现）  
**工作量**: 大  
**修改风险**: 中

---

### 4.6 大数据性能

**原建议**: P2.5 - 优化大数据量性能

**重新评估**:
- ✅ 当前限制为 5000 行
- ❓ 需明确：
  - 是硬性截断、抽样还是拒绝分析？
  - 用户是否得到清晰提示？
  - 5000 行之后的数据是否可能被误以为参与分析？
  - 哪些算法构成主要性能瓶颈？
  - 是否真的需要 Web Worker？
  - 在优化前应建立什么性能基线？

**当前状态**:
- 5000 行为硬性截断（`MAX_ROWS = 5000`）
- 用户得到警告提示（"仅基于前 5000 行数据计算"）
- 主要性能瓶颈：相关性分析 O(n²)、图表渲染

**新建议**:
- 建立性能基线（测试当前 5000 行数据的处理时间）
- 识别性能瓶颈（使用性能分析工具）
- 优先优化算法复杂度，其次考虑 Web Worker

**优先级**: P2 - 中  
**工作量**: 大  
**修改风险**: 中

---

## 五、关键问题总结

### 5.1 P0 问题（导致崩溃、错误结果、数据污染）

**✅ 无 P0 问题**

项目可以正常启动和运行，无严重数据错误或安全风险。

---

### 5.2 P1 问题（核心体验和通用性缺失）

#### P1.1 字段分类系统教育偏向

**问题**: `fieldClassifier.ts` 包含大量教育关键词，导致非教育数据被错误分类

**影响**: 所有非教育数据的分析结果可能错误

**推荐处理**: 参数化，建立通用字段类型系统

**修改风险**: 中

---

#### P1.2 维度识别教育偏向

**问题**: `metricLayer.ts` 维度关键词偏向教育（班级、学校、科类）

**影响**: 非教育数据的分组分析可能失败

**推荐处理**: 参数化，扩展维度关键词列表

**修改风险**: 低

---

#### P1.3 指标方向硬编码

**问题**: `metricLayer.ts` 中 rank 字段默认为 lower-is-better

**影响**: 非教育数据的排名计算可能错误

**推荐处理**: 参数化，允许用户自定义指标方向

**修改风险**: 低

---

#### P1.4 缺少 Excel 导出

**问题**: 仅支持 CSV 导出，不支持 Excel 导出

**影响**: 用户体验差，无法导出带格式的数据

**推荐处理**: 使用现有 `xlsx` 库实现 Excel 导出

**修改风险**: 低

---

### 5.3 P2 问题（架构、性能、可维护性）

#### P2.1 字段推荐逻辑教育偏向

**问题**: `useParsedTable.ts` 推荐字段偏向教育（primaryTotal、rank、courseScore）

**影响**: 非教育数据用户需要手动选择分析字段

**推荐处理**: 参数化，基于字段类型和数据分布推荐

**修改风险**: 中

---

#### P2.2 缺少数据清洗能力

**问题**: 缺少缺失值处理、重复值处理、字段类型修正

**影响**: 数据质量无法保证

**推荐处理**: 实现数据清洗预览和交互式修正

**修改风险**: 中

---

#### P2.3 缺少日期分析能力

**问题**: 支持日期识别，但无日期分析（时间序列、趋势分析）

**影响**: 时间序列数据无法深入分析

**推荐处理**: 实现日期字段的时间序列分析

**修改风险**: 中

---

#### P2.4 大数据性能瓶颈

**问题**: 相关性分析 O(n²) 复杂度，图表渲染大量数据可能卡顿

**影响**: 大数据量用户体验差

**推荐处理**: 优化算法复杂度，引入 Web Worker

**修改风险**: 中

---

### 5.4 P3 问题（高级功能）

#### P3.1 假设检验

**问题**: 不支持假设检验（t 检验、卡方检验等）

**影响**: 无法进行统计显著性检验

**推荐处理**: 建立统计检验框架，提供前提条件检查

**修改风险**: 中

---

#### P3.2 多文件合并

**问题**: 不支持多个文件合并分析

**影响**: 无法进行跨文件分析

**推荐处理**: 实现文件合并逻辑和字段映射

**修改风险**: 中

---

#### P3.3 透视表

**问题**: 不支持透视表分析

**影响**: 无法进行交叉分析

**推荐处理**: 实现透视表引擎和 UI

**修改风险**: 中

---

## 六、数据量管道审计发现（2026-07-21 补充）

### 6.1 核心问题

基于对源码的完整审计，发现以下数据量管理问题：

#### 6.1.1 20000行解析截断无警告

**文件**: `src/utils/tableParser/workbook.ts` (第100行)

**问题**:
```typescript
const trimmedData = rawData.slice(0, MAX_ROWS);  // MAX_ROWS = 20000
```

**当前行为**:
- 超过20000行时，数据被静默截断
- 未保存原始总行数 `rawData.length`
- 未生成任何警告提示用户

**影响**:
- 用户无法区分"原始20000行"和"实际20000行"
- 数据丢失无感知

**修复优先级**: 🔴 高

---

#### 6.1.2 5000行警告被覆盖

**文件**: `src/hooks/useParsedTable.ts` (第91-99行), `src/App.tsx` (第362行)

**问题**:
```typescript
// App.tsx: 文件上传后
setRawText(text);  // 触发 useParsedTable 的 useEffect

// useParsedTable.ts: useEffect([rawText])
useEffect(() => {
  if (!rawText.trim()) return;
  const result = parseTableText(rawText);
  setParsedData(result);
  setParseWarnings(result.warnings || []);  // 覆盖！5000行警告丢失
}, [rawText]);
```

**当前行为**:
- 文件上传后，`setRawText(text)` 触发 `useEffect([rawText])`
- `useEffect` 调用 `parseTableText(rawText)` 重新解析
- 重新解析覆盖 `parseWarnings`，导致5000行警告丢失

**影响**:
- 文件上传超过5000行时，警告不显示
- Sheet切换超过5000行时，警告不显示

**修复优先级**: 🔴 高

---

#### 6.1.3 分析样本不一致

**文件**: 4个模块独立执行5000行截断

| 模块 | 文件 | 行号 |
|------|------|------|
| analysisEngine.ts | extractFieldValues | 48 |
| correlationAnalyzer.ts | extractColumnVectors | 194 |
| groupByDimension.ts | groupByDimension | 139 |
| GeneralDataOverview.tsx | computeOverview | 35 |

**当前行为**:
- 4个模块独立执行 `rows.slice(0, MAX_ROWS)`
- 无法保证所有分析使用同一批数据

**影响**:
- 可能导致统计结果不一致
- 无法统一控制抽样策略

**修复优先级**: 🟡 中

---

#### 6.1.4 reparseSheet 丢失风险

**文件**: `src/utils/fileImport.ts` (第45-47行), `src/hooks/useParsedTable.ts` (第95行)

**问题**:
```typescript
// fileImport.ts: 创建 reparseSheet 闭包
reparseSheet: async (sheetName: string) => {
  return parseTableFile(file, sheetName);
}

// useParsedTable.ts: 二次解析覆盖
const result = parseTableText(rawText);
setParsedData(result);  // result 是 ParsedTable 类型，不含 reparseSheet
```

**当前行为**:
- 文件上传后，`parsedData` 包含 `reparseSheet` 方法
- `setRawText(text)` 触发二次解析
- 二次解析返回 `ParsedTable` 类型，不包含 `reparseSheet`

**影响**:
- 连续切换Sheet时，`reparseSheet` 可能不存在

**修复优先级**: 🟡 中

---

#### 6.1.5 数据量概念混淆

**问题**:
- 仅使用 `rows.length` 表示所有数据量概念
- 无法区分：
  - 原始文件或原始输入总行数
  - 解析器实际读取行数
  - 有效数据行数
  - 筛选后行数
  - 实际参与分析的行数
  - 是否发生解析阶段截断
  - 是否发生分析阶段抽样

**影响**:
- 无法准确展示数据量状态
- 无法实现统一的抽样策略

**修复优先级**: 🔴 高

---

### 6.2 调用链分析

#### 6.2.1 文件上传调用链

```
用户选择文件
  ↓
App.tsx: handleFileUpload() (第321行)
  ↓
parseTableFile(file) [src/utils/fileImport.ts:11]
  ↓
parseWorkbook() [src/utils/tableParser/workbook.ts:29]
  ↓
【截断点1】rawData.slice(0, MAX_ROWS) (第100行, MAX_ROWS=20000)
  ↓
返回 ParsedTableResult
  ↓
App.tsx: setParsedData(result) (第340行)
  ↓
App.tsx: setParseWarnings([...result.warnings, '5000行警告']) (第346行)
  ↓
【关键操作】App.tsx: setRawText(text) (第362行)
  ↓
useParsedTable.ts: useEffect([rawText]) (第91行)
  ↓
【二次解析】parseTableText(rawText) (第94行)
  ↓
setParseWarnings(result.warnings) (第96行) - 覆盖！5000行警告丢失
```

#### 6.2.2 Sheet切换调用链

```
用户点击Sheet按钮
  ↓
App.tsx: handleSheetChange(sheetName) (第375行)
  ↓
parsedData.reparseSheet(sheetName) (第380行)
  ↓
parseTableFile(file, sheetName)
  ↓
【截断点2】rawData.slice(0, MAX_ROWS) [新Sheet也受20000行限制]
  ↓
App.tsx: setParsedData(result) (第382行)
  ↓
App.tsx: setParseWarnings(result.warnings) (第383行)
  ↓
App.tsx: setRawText(text) (第392行)
  ↓
useParsedTable.ts: useEffect([rawText]) (第91行)
  ↓
【二次解析】parseTableText(rawText) (第94行)
  ↓
setParseWarnings(result.warnings) (第96行) - 再次覆盖
```

---

### 6.3 截断位置汇总

| 截断点 | 文件 | 行号 | 限制 | 是否生成警告 | 是否保存原始行数 |
|--------|------|------|------|------------|----------------|
| 1 | workbook.ts | 100 | 20000行 | ❌ 否 | ❌ 否 |
| 2 | analysisEngine.ts | 48 | 5000行 | ❌ 否 | ✅ 是（truncatedRows） |
| 3 | correlationAnalyzer.ts | 194 | 5000行 | ❌ 否 | ❌ 否 |
| 4 | groupByDimension.ts | 139 | 5000行 | ❌ 否 | ❌ 否 |
| 5 | GeneralDataOverview.tsx | 35 | 5000行 | ✅ 是（组件内警告） | ❌ 否 |

**总计**: 5个独立截断位置，1个生成警告，4个静默截断

---

### 6.4 解决方案方向

详见 [NEXT_SLICE_PROPOSAL.md](./NEXT_SLICE_PROPOSAL.md) 和 [PLATFORM_UPGRADE_ROADMAP.md](./PLATFORM_UPGRADE_ROADMAP.md) 中的 Stage 0A。

核心解决方向：
1. 建立 DataVolumeState 数据量状态模型
2. 解决20000行解析截断问题（保存原始行数、生成警告）
3. 解决5000行警告被覆盖问题（禁用自动重解析）
4. 实现确定性抽样（系统抽样）
5. 统一分析数据集（生成唯一 analysisRows）
6. 超过5000行时停止自动分析，要求用户确认
7. 持久展示抽样状态

---

## 七、附录

### 6.1 教育关键词完整清单

**文件**: `src/utils/tableParser/fieldClassifier.ts`

#### IDENTITY_KEYWORDS (18个)
```typescript
const IDENTITY_KEYWORDS = [
  '学校代码', '学校名称', '姓名', '班级', '考号', '座号', '学号',
  '考生号', '准考证', '考生姓名', '身份证号', '性别', '民族',
  '院系', '专业', '行政班', '教学班',
];
```

#### SCORE_KEYWORDS (27个)
```typescript
const SCORE_KEYWORDS = [
  '总分', '语文', '数学', '英语', '外语', '物理', '化学', '生物',
  '政治', '历史', '地理', '成绩', '分数', '得分',
  '总分（不含加分）', '原始总分', '标准总分',
  '综合', '文科综合', '理科综合',
  '高考成绩', '综合成绩', '赋分后成绩', '赋分前成绩', '语数英总',
  '等级分', '标准分', '原始分', '转换分',
];
```

#### RANK_KEYWORDS (9个)
```typescript
const RANK_KEYWORDS = [
  '名次', '排名', '位次', '年级名次', '班级名次',
  '校排', '班排', '年排', '级排',
];
```

#### BONUS_KEYWORDS (8个)
```typescript
const BONUS_KEYWORDS = [
  '加分', '区内加分', '区外加分', '政策加分', '优惠加分', '特长加分',
  '奖励分', '奖励',
];
```

#### PENALTY_KEYWORDS (3个)
```typescript
const PENALTY_KEYWORDS = [
  '扣分', '违纪扣分', '惩罚',
];
```

#### CATEGORY_KEYWORDS (10个)
```typescript
const CATEGORY_KEYWORDS = [
  '组合', '组合简称', '科类', '选科', '类别', '文理', '科类名称',
  '选考', '首选', '再选',
];
```

#### PRIMARY_TOTAL_KEYWORDS (9个)
```typescript
const PRIMARY_TOTAL_KEYWORDS = [
  '总分', '总成绩', '综合成绩', '总评', '最终成绩',
  '高考成绩', '赋分后成绩', '赋分前成绩', '语数英总',
  '等级分', '标准分',
];
```

#### SECTION_TOTAL_KEYWORDS (6个)
```typescript
const SECTION_TOTAL_KEYWORDS = [
  '合计', '总计', '小计', '模块合计', '模块总分', '类别总分',
];
```

**总计**: 90 个教育相关关键词

---

### 6.2 通用字段类型建议

**建议新增的通用字段类型**:

```typescript
type GeneralFieldType =
  | 'numeric'      // 通用数值（金额、数量、比率等）
  | 'currency'     // 货币
  | 'percentage'   // 百分比
  | 'date'         // 日期
  | 'time'         // 时间
  | 'datetime'     // 日期时间
  | 'category'     // 分类变量
  | 'ordinal'      // 有序分类
  | 'identifier'   // 标识符
  | 'text'         // 文本
  | 'boolean'      // 布尔值
  | 'unknown';     // 未知
```

---

**分析完成时间**: 2026-07-20  
**分析人员**: AI Assistant  
**分析状态**: ✅ 完成
