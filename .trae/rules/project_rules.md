# 项目规则

## 公告更新规则（updateLogs.ts）

两次部署之间做的优化或新功能需要更新公告，但需遵守以下规则：

1. **只写重要内容**：不是所有改动都要写，只写用户能感知到的优化或新功能
2. **不用专业术语**：用用户能理解的语言描述
3. **非重大 bug 不写修复**：普通 bug 修复不需要出现在公告中
4. **必须写最新日期**：
   - 如果当天已有版本公告，可以追加到该版本
   - 如果没有，必须创建新版本号（patch +1）和新日期
   - **绝对不要修改旧版本的公告内容**
5. **版本隔离**：每个版本的公告一旦创建就是不可变的
